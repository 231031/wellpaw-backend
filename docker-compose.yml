services:
  wellpaw_backend:
    container_name: wellpaw-api
    build: .
    env_file:
      - .env
    restart: unless-stopped
    ports:
      - "50001:50001"
    volumes:
      - ./public:/app/go/public
      - ./:/app/go
    depends_on:
      wellpaw_db:
        condition: service_healthy
    networks:
      - wellpaw-shared-nw

  wellpaw_db:
    image: postgres:17-alpine
    restart: unless-stopped
    container_name: wellpaw-db
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - "5432:5432"
    env_file:
      - .env
    volumes:
      - wellpaw_db_volumes:/var/lib/postgresql/data
    networks:
      - wellpaw-shared-nw
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}" ]
      interval: 5s
      timeout: 5s
      retries: 5
  
  wellpaw_redis:
    image: redis:7.2-alpine # Note: redis 8 is in early dev, 7.2 is stable for TLS
    container_name: wellpaw-redis
    env_file:
      - .env
    ports:
      - "6379:6379"
    volumes:
      - wellpaw_cache:/data
      - ./secrets/redis_cert.pem:/tls/cert.pem
      - ./secrets/redis_key.pem:/tls/key.pem
    restart: unless-stopped
    command: >
      redis-server 
      --requirepass ${REDIS_PASSWORD}
      --tls-port 6379
      --port 0
      --tls-cert-file /tls/cert.pem
      --tls-key-file /tls/key.pem
      --tls-auth-clients no
    healthcheck:
      test: ["CMD", "redis-cli", "--tls", "--insecure", "-a", "${REDIS_PASSWORD}", "ping"]
    networks:
      - wellpaw-shared-nw

networks:
  wellpaw-shared-nw:
    external: true
volumes:
  wellpaw_db_volumes:
  wellpaw_cache: